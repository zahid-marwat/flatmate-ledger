{
  "name": "flatmate-ledger",
  "private": true,
  "version": "0.1.0",
  "description": "House-focused expense tracker for shared flats in Pakistan.",
  "type": "module",
  "scripts": {
    "start": "node src/api/server.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
